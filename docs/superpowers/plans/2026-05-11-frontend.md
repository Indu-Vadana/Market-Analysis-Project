# Trading Dashboard — Next.js Frontend Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a dark-themed Next.js dashboard with a live-price sidebar (NIFTY, crypto, forex + watchlist) and a categorized news feed (Trading, AI/Tech, Energy/Space).

**Architecture:** Next.js 14 app router fetches from the FastAPI backend on an interval. Sidebar shows live market prices and personal watchlist. Main feed shows news cards tagged by category with color-coded labels. No auth — single user personal tool.

**Tech Stack:** Next.js 14, TailwindCSS, shadcn/ui, TypeScript, Vercel

**Pre-requisite:** Backend deployed on Railway. Set `NEXT_PUBLIC_API_URL` before running.

---

## File Map

```
frontend/
├── app/
│   ├── layout.tsx          # Root layout, dark theme, global fonts
│   ├── page.tsx            # Dashboard: composes Sidebar + NewsFeed
│   └── globals.css         # TailwindCSS base
├── components/
│   ├── Sidebar.tsx         # Left panel: indices + crypto + forex + watchlist
│   ├── PriceRow.tsx        # Single price line (symbol, price, change%)
│   ├── NewsFeed.tsx        # Right panel: category tabs + news cards
│   ├── NewsCard.tsx        # Single article card (title, source, tag, time)
│   └── WatchlistManager.tsx # Add/remove tickers UI
├── lib/
│   └── api.ts              # Typed fetch helpers for all backend endpoints
├── hooks/
│   └── useInterval.ts      # setInterval hook for polling
├── types.ts                # Shared TypeScript interfaces
├── tailwind.config.ts
├── next.config.ts
└── .env.local.example
```

---

## Task 1: Scaffold Next.js App

**Files:**
- Create: `frontend/` (entire scaffold)
- Create: `frontend/.env.local.example`

- [ ] **Step 1: Create Next.js app**

```bash
cd /Users/santhosh/Projects/trading-news-site
npx create-next-app@latest frontend --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"
cd frontend
```

- [ ] **Step 2: Install shadcn/ui**

```bash
npx shadcn@latest init
```

When prompted: style=Default, base color=Slate, CSS variables=yes.

- [ ] **Step 3: Install shadcn components used in this project**

```bash
npx shadcn@latest add button input badge tabs
```

- [ ] **Step 4: Create .env.local.example**

```
# frontend/.env.local.example
NEXT_PUBLIC_API_URL=https://your-railway-app.railway.app
```

Copy to `.env.local` and set the real URL (use `http://localhost:8000` for local dev).

- [ ] **Step 5: Set dark background in globals.css**

Replace the `:root` and `body` defaults in `app/globals.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --background: #0f1117;
  --foreground: #e0e0e0;
}

body {
  background-color: var(--background);
  color: var(--foreground);
  font-family: 'Inter', sans-serif;
}
```

- [ ] **Step 6: Verify dev server starts**

```bash
npm run dev
```

Open http://localhost:3000 — should show Next.js default page on dark background.

- [ ] **Step 7: Commit**

```bash
git add frontend/
git commit -m "feat: scaffold Next.js frontend with TailwindCSS and shadcn/ui"
```

---

## Task 2: TypeScript Types and API Client

**Files:**
- Create: `frontend/types.ts`
- Create: `frontend/lib/api.ts`
- Create: `frontend/hooks/useInterval.ts`

- [ ] **Step 1: Create types.ts**

```typescript
// frontend/types.ts
export interface MarketPrice {
  id: number;
  symbol: string;
  price: number;
  change_pct: number;
  category: "india" | "crypto" | "forex";
}

export interface NewsItem {
  id: number;
  title: string;
  url: string;
  source: string;
  category: "trading" | "tech" | "energy";
  published_at: string | null;
}

export interface WatchlistItem {
  id: number;
  symbol: string;
  type: "stock" | "crypto" | "forex";
  added_at: string;
}
```

- [ ] **Step 2: Create lib/api.ts**

```typescript
// frontend/lib/api.ts
import { MarketPrice, NewsItem, WatchlistItem } from "@/types";

const BASE = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000";

async function fetchJSON<T>(path: string, options?: RequestInit): Promise<T> {
  const res = await fetch(`${BASE}${path}`, options);
  if (!res.ok) throw new Error(`API error ${res.status}: ${path}`);
  return res.json();
}

export const api = {
  getMarket: () => fetchJSON<MarketPrice[]>("/market"),
  getNews: (category: "trading" | "tech" | "energy") =>
    fetchJSON<NewsItem[]>(`/news?category=${category}`),
  getWatchlist: () => fetchJSON<WatchlistItem[]>("/watchlist"),
  addToWatchlist: (symbol: string, type: string) =>
    fetchJSON<{ status: string }>("/watchlist", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ symbol, type }),
    }),
  removeFromWatchlist: (symbol: string) =>
    fetchJSON<{ status: string }>(`/watchlist/${symbol}`, { method: "DELETE" }),
};
```

- [ ] **Step 3: Create hooks/useInterval.ts**

```typescript
// frontend/hooks/useInterval.ts
import { useEffect, useRef } from "react";

export function useInterval(callback: () => void, delay: number) {
  const savedCallback = useRef(callback);
  useEffect(() => { savedCallback.current = callback; }, [callback]);
  useEffect(() => {
    const id = setInterval(() => savedCallback.current(), delay);
    return () => clearInterval(id);
  }, [delay]);
}
```

- [ ] **Step 4: Verify TypeScript compiles**

```bash
npx tsc --noEmit
```

Expected: no errors

- [ ] **Step 5: Commit**

```bash
git add frontend/types.ts frontend/lib/api.ts frontend/hooks/
git commit -m "feat: TypeScript types, API client, useInterval hook"
```

---

## Task 3: PriceRow Component

**Files:**
- Create: `frontend/components/PriceRow.tsx`

- [ ] **Step 1: Create PriceRow.tsx**

```tsx
// frontend/components/PriceRow.tsx
import { MarketPrice } from "@/types";

interface Props {
  item: MarketPrice;
}

export function PriceRow({ item }: Props) {
  const isUp = item.change_pct >= 0;
  const changeColor = isUp ? "text-emerald-400" : "text-red-400";
  const arrow = isUp ? "▲" : "▼";

  return (
    <div className="flex items-center justify-between py-1 px-2 rounded hover:bg-white/5 transition-colors">
      <span className="text-slate-300 text-sm font-mono font-medium">{item.symbol}</span>
      <div className="text-right">
        <div className="text-white text-sm font-mono">
          {item.price.toLocaleString("en-IN", { maximumFractionDigits: 2 })}
        </div>
        <div className={`text-xs font-mono ${changeColor}`}>
          {arrow} {Math.abs(item.change_pct).toFixed(2)}%
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npx tsc --noEmit
```

Expected: no errors

- [ ] **Step 3: Commit**

```bash
git add frontend/components/PriceRow.tsx
git commit -m "feat: PriceRow component"
```

---

## Task 4: WatchlistManager Component

**Files:**
- Create: `frontend/components/WatchlistManager.tsx`

- [ ] **Step 1: Create WatchlistManager.tsx**

```tsx
// frontend/components/WatchlistManager.tsx
"use client";
import { useState } from "react";
import { WatchlistItem } from "@/types";
import { api } from "@/lib/api";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { PriceRow } from "./PriceRow";

interface Props {
  items: WatchlistItem[];
  onUpdate: () => void;
}

export function WatchlistManager({ items, onUpdate }: Props) {
  const [symbol, setSymbol] = useState("");
  const [type, setType] = useState<"stock" | "crypto" | "forex">("stock");
  const [loading, setLoading] = useState(false);

  async function handleAdd() {
    if (!symbol.trim()) return;
    setLoading(true);
    try {
      await api.addToWatchlist(symbol.trim().toUpperCase(), type);
      setSymbol("");
      onUpdate();
    } catch (e) {
      console.error(e);
    } finally {
      setLoading(false);
    }
  }

  async function handleRemove(sym: string) {
    await api.removeFromWatchlist(sym);
    onUpdate();
  }

  return (
    <div>
      <div className="text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2 px-2">
        Watchlist
      </div>

      {items.map((item) => (
        <div key={item.id} className="flex items-center justify-between py-1 px-2 group">
          <span className="text-slate-300 text-sm font-mono">{item.symbol}</span>
          <div className="flex items-center gap-1">
            <span className="text-xs text-slate-500">{item.type}</span>
            <button
              onClick={() => handleRemove(item.symbol)}
              className="text-slate-600 hover:text-red-400 opacity-0 group-hover:opacity-100 transition-opacity text-xs ml-1"
            >
              ✕
            </button>
          </div>
        </div>
      ))}

      <div className="mt-3 px-2 space-y-2">
        <Input
          value={symbol}
          onChange={(e) => setSymbol(e.target.value)}
          onKeyDown={(e) => e.key === "Enter" && handleAdd()}
          placeholder="Add ticker (e.g. RELIANCE)"
          className="h-7 text-xs bg-slate-800 border-slate-700 text-white placeholder:text-slate-500"
        />
        <div className="flex gap-1">
          {(["stock", "crypto", "forex"] as const).map((t) => (
            <button
              key={t}
              onClick={() => setType(t)}
              className={`text-xs px-2 py-0.5 rounded transition-colors ${
                type === t ? "bg-indigo-600 text-white" : "bg-slate-700 text-slate-400 hover:bg-slate-600"
              }`}
            >
              {t}
            </button>
          ))}
        </div>
        <Button
          onClick={handleAdd}
          disabled={loading || !symbol.trim()}
          className="w-full h-7 text-xs bg-indigo-600 hover:bg-indigo-700"
        >
          {loading ? "Adding..." : "Add"}
        </Button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npx tsc --noEmit
```

Expected: no errors

- [ ] **Step 3: Commit**

```bash
git add frontend/components/WatchlistManager.tsx
git commit -m "feat: WatchlistManager component with add/remove"
```

---

## Task 5: Sidebar Component

**Files:**
- Create: `frontend/components/Sidebar.tsx`

- [ ] **Step 1: Create Sidebar.tsx**

```tsx
// frontend/components/Sidebar.tsx
"use client";
import { useEffect, useState, useCallback } from "react";
import { MarketPrice, WatchlistItem } from "@/types";
import { api } from "@/lib/api";
import { useInterval } from "@/hooks/useInterval";
import { PriceRow } from "./PriceRow";
import { WatchlistManager } from "./WatchlistManager";

export function Sidebar() {
  const [market, setMarket] = useState<MarketPrice[]>([]);
  const [watchlist, setWatchlist] = useState<WatchlistItem[]>([]);

  const fetchMarket = useCallback(async () => {
    try {
      const data = await api.getMarket();
      setMarket(data);
    } catch (e) {
      console.error(e);
    }
  }, []);

  const fetchWatchlist = useCallback(async () => {
    try {
      const data = await api.getWatchlist();
      setWatchlist(data);
    } catch (e) {
      console.error(e);
    }
  }, []);

  useEffect(() => {
    fetchMarket();
    fetchWatchlist();
  }, [fetchMarket, fetchWatchlist]);

  useInterval(fetchMarket, 60_000); // refresh every 60s

  const india = market.filter((m) => m.category === "india");
  const crypto = market.filter((m) => m.category === "crypto").slice(0, 6);
  const forex = market.filter((m) => m.category === "forex");

  function SectionLabel({ label }: { label: string }) {
    return (
      <div className="text-xs font-semibold text-slate-500 uppercase tracking-wider mt-4 mb-1 px-2">
        {label}
      </div>
    );
  }

  return (
    <aside className="w-52 shrink-0 bg-[#13151f] border-r border-slate-800 h-screen overflow-y-auto py-4 flex flex-col">
      <div className="px-2 mb-4">
        <h1 className="text-sm font-bold text-white">Trading Dashboard</h1>
        <p className="text-xs text-slate-500">Live market data</p>
      </div>

      <SectionLabel label="India" />
      {india.map((m) => <PriceRow key={m.symbol} item={m} />)}

      <SectionLabel label="Crypto" />
      {crypto.map((m) => <PriceRow key={m.symbol} item={m} />)}

      <SectionLabel label="Forex" />
      {forex.map((m) => <PriceRow key={m.symbol} item={m} />)}

      <div className="mt-4 border-t border-slate-800 pt-4">
        <WatchlistManager items={watchlist} onUpdate={fetchWatchlist} />
      </div>
    </aside>
  );
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npx tsc --noEmit
```

Expected: no errors

- [ ] **Step 3: Commit**

```bash
git add frontend/components/Sidebar.tsx
git commit -m "feat: Sidebar with live prices and watchlist"
```

---

## Task 6: NewsCard and NewsFeed Components

**Files:**
- Create: `frontend/components/NewsCard.tsx`
- Create: `frontend/components/NewsFeed.tsx`

- [ ] **Step 1: Create NewsCard.tsx**

```tsx
// frontend/components/NewsCard.tsx
import { NewsItem } from "@/types";

const CATEGORY_CONFIG = {
  trading: { label: "Trading", color: "bg-indigo-600 text-white" },
  tech: { label: "AI & Tech", color: "bg-emerald-600 text-white" },
  energy: { label: "Energy", color: "bg-orange-500 text-white" },
};

interface Props {
  item: NewsItem;
}

export function NewsCard({ item }: Props) {
  const config = CATEGORY_CONFIG[item.category];
  const timeAgo = item.published_at
    ? new Date(item.published_at).toLocaleTimeString("en-IN", { hour: "2-digit", minute: "2-digit" })
    : "";

  return (
    <a
      href={item.url}
      target="_blank"
      rel="noopener noreferrer"
      className="block bg-[#13151f] border border-slate-800 rounded-lg p-3 hover:border-slate-600 hover:bg-[#1a1d2e] transition-all"
    >
      <div className="flex items-start gap-2 mb-2">
        <span className={`text-xs px-2 py-0.5 rounded font-medium shrink-0 ${config.color}`}>
          {config.label}
        </span>
        <span className="text-xs text-slate-500 ml-auto shrink-0">{timeAgo}</span>
      </div>
      <p className="text-sm text-slate-200 leading-snug line-clamp-2">{item.title}</p>
      <p className="text-xs text-slate-500 mt-1">{item.source}</p>
    </a>
  );
}
```

- [ ] **Step 2: Create NewsFeed.tsx**

```tsx
// frontend/components/NewsFeed.tsx
"use client";
import { useEffect, useState, useCallback } from "react";
import { NewsItem } from "@/types";
import { api } from "@/lib/api";
import { useInterval } from "@/hooks/useInterval";
import { NewsCard } from "./NewsCard";

type Category = "trading" | "tech" | "energy";

const TABS: { key: Category; label: string }[] = [
  { key: "trading", label: "📈 Trading" },
  { key: "tech", label: "🤖 AI & Tech" },
  { key: "energy", label: "⚡ Energy" },
];

export function NewsFeed() {
  const [activeTab, setActiveTab] = useState<Category>("trading");
  const [news, setNews] = useState<Record<Category, NewsItem[]>>({
    trading: [],
    tech: [],
    energy: [],
  });

  const fetchAll = useCallback(async () => {
    const [trading, tech, energy] = await Promise.allSettled([
      api.getNews("trading"),
      api.getNews("tech"),
      api.getNews("energy"),
    ]);
    setNews({
      trading: trading.status === "fulfilled" ? trading.value : [],
      tech: tech.status === "fulfilled" ? tech.value : [],
      energy: energy.status === "fulfilled" ? energy.value : [],
    });
  }, []);

  useEffect(() => { fetchAll(); }, [fetchAll]);
  useInterval(fetchAll, 300_000); // refresh every 5 min

  return (
    <main className="flex-1 p-6 overflow-y-auto">
      <div className="flex gap-2 mb-6">
        {TABS.map((tab) => (
          <button
            key={tab.key}
            onClick={() => setActiveTab(tab.key)}
            className={`px-4 py-1.5 rounded-full text-sm font-medium transition-colors ${
              activeTab === tab.key
                ? "bg-indigo-600 text-white"
                : "bg-slate-800 text-slate-400 hover:bg-slate-700"
            }`}
          >
            {tab.label}
          </button>
        ))}
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-3">
        {news[activeTab].length === 0 ? (
          <p className="text-slate-500 text-sm col-span-3">Loading news...</p>
        ) : (
          news[activeTab].map((item) => <NewsCard key={item.id} item={item} />)
        )}
      </div>
    </main>
  );
}
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
npx tsc --noEmit
```

Expected: no errors

- [ ] **Step 4: Commit**

```bash
git add frontend/components/NewsCard.tsx frontend/components/NewsFeed.tsx
git commit -m "feat: NewsCard and NewsFeed with category tabs"
```

---

## Task 7: Assemble Dashboard Page

**Files:**
- Modify: `frontend/app/page.tsx`
- Modify: `frontend/app/layout.tsx`

- [ ] **Step 1: Update layout.tsx**

```tsx
// frontend/app/layout.tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Trading Dashboard",
  description: "Personal trading news and market data dashboard",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.className} bg-[#0f1117] text-slate-200 antialiased`}>
        {children}
      </body>
    </html>
  );
}
```

- [ ] **Step 2: Update page.tsx**

```tsx
// frontend/app/page.tsx
import { Sidebar } from "@/components/Sidebar";
import { NewsFeed } from "@/components/NewsFeed";

export default function Home() {
  return (
    <div className="flex h-screen overflow-hidden">
      <Sidebar />
      <NewsFeed />
    </div>
  );
}
```

- [ ] **Step 3: Start dev server and verify full dashboard renders**

```bash
npm run dev
```

Open http://localhost:3000.

Expected:
- Dark background
- Left sidebar with "India / Crypto / Forex" sections (prices if backend running, empty otherwise)
- Right area with 3 tab buttons (Trading / AI & Tech / Energy)
- News cards appear when backend is running

- [ ] **Step 4: Commit**

```bash
git add frontend/app/
git commit -m "feat: assemble dashboard page with Sidebar and NewsFeed"
```

---

## Task 8: Deploy to Vercel

- [ ] **Step 1: Push to GitHub (if not already done)**

```bash
git push origin main
```

- [ ] **Step 2: Import project in Vercel**

1. Go to vercel.com → New Project → Import from GitHub
2. Select `trading-news-site` repo
3. Set **Root Directory** to `frontend`
4. Framework Preset: Next.js (auto-detected)

- [ ] **Step 3: Add environment variable**

In Vercel project → Settings → Environment Variables:
```
NEXT_PUBLIC_API_URL = https://your-railway-app.railway.app
```

- [ ] **Step 4: Deploy**

Click Deploy. Wait ~2 min.

- [ ] **Step 5: Verify on Vercel URL**

Open the Vercel URL in your browser.

Expected:
- Sidebar shows live NIFTY, BTC, USD/INR prices
- News cards load in all 3 tabs
- Watchlist add/remove works
- No console errors

- [ ] **Step 6: Verify on mobile**

Open the Vercel URL on your phone. Check that the layout doesn't break.

---

## Verification Checklist

- [ ] Sidebar shows NIFTY, SENSEX, BTC, ETH, USD/INR with live prices
- [ ] Green ▲ / red ▼ colors show correctly for positive/negative change
- [ ] Trading tab shows market/financial news
- [ ] AI & Tech tab shows Nvidia, Anthropic, OpenAI type stories
- [ ] Energy tab shows renewable/space energy stories
- [ ] Add "RELIANCE" stock to watchlist → appears in sidebar
- [ ] Remove from watchlist → disappears
- [ ] Prices auto-refresh (wait 60s, prices update without page reload)
- [ ] Works on mobile screen
- [ ] `npm run build` completes with no errors
